import { StyleConfig } from "@chakra-ui/theme-tools";

export const Link: StyleConfig = {
    baseStyle: {
        transition: "none",
        _focus: {
            boxShadow: "none",
        },
    },
};
